from django.apps import AppConfig


class SkillsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "hiscores.skills"

"""     def ready(self):
        import hiscores.skills.signals """
